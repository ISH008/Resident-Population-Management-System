# Resident Management Frontend

## Run
1. Install dependencies:
   - `npm install`
2. Start dev server:
   - `npm run dev`
3. Open:
   - `http://localhost:5173`

## Notes
- Dev proxy forwards `/api` to `http://localhost:8080`.
- Ensure backend is running before frontend login.
- Admin pages: residents, users, audit logs.
