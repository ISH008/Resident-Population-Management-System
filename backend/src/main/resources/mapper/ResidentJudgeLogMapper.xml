<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.population.resident.mapper.ResidentJudgeLogMapper">

    <insert id="insert" parameterType="com.population.resident.domain.ResidentJudgeLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO resident_judge_log (
            resident_id, rule_id, rule_code, hit_flag, score_delta, final_score, final_status,
            judge_reason, judge_version, operator_id
        ) VALUES (
            #{residentId}, #{ruleId}, #{ruleCode}, #{hitFlag}, #{scoreDelta}, #{finalScore}, #{finalStatus},
            #{judgeReason}, #{judgeVersion}, #{operatorId}
        )
    </insert>

    <select id="findByResidentId" resultType="com.population.resident.domain.ResidentJudgeLog">
        SELECT
            id,
            resident_id,
            rule_id,
            rule_code,
            hit_flag,
            score_delta,
            final_score,
            final_status,
            judge_reason,
            judge_version,
            judge_time,
            operator_id
        FROM resident_judge_log
        WHERE resident_id = #{residentId}
        ORDER BY id DESC
    </select>
</mapper>
