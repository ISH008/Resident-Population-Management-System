<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.population.resident.mapper.ResidentMapper">

    <sql id="baseWhere">
        FROM resident
        WHERE is_deleted = 0
        <if test="name != null and name != ''">
            AND name LIKE CONCAT('%', #{name}, '%')
        </if>
        <if test="idCard != null and idCard != ''">
            AND id_card = #{idCard}
        </if>
        <if test="residenceStatus != null and residenceStatus != ''">
            AND residence_status = #{residenceStatus}
        </if>
    </sql>

    <select id="pageQuery" resultType="com.population.resident.domain.Resident">
        SELECT
            id,
            name,
            id_card,
            gender,
            birthday,
            phone,
            actual_address,
            address_province,
            address_city,
            address_district,
            address_detail,
            residence_type,
            status,
            residence_status,
            residence_score,
            stay_start_date,
            stay_end_date,
            is_local_hukou,
            proof_type,
            last_judge_time,
            judge_version,
            judge_reason,
            created_at
        <include refid="baseWhere"/>
        ORDER BY id DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(1)
        <include refid="baseWhere"/>
    </select>

    <select id="findById" resultType="com.population.resident.domain.Resident">
        SELECT
            id,
            name,
            id_card,
            gender,
            birthday,
            phone,
            actual_address,
            address_province,
            address_city,
            address_district,
            address_detail,
            residence_type,
            status,
            residence_status,
            residence_score,
            stay_start_date,
            stay_end_date,
            is_local_hukou,
            proof_type,
            last_judge_time,
            judge_version,
            judge_reason,
            created_at
        FROM resident
        WHERE id = #{id}
          AND is_deleted = 0
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.population.resident.domain.Resident" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO resident (
            name, id_card, gender, birthday, phone, actual_address, address_province, address_city, address_district, address_detail, residence_type, status,
            stay_start_date, stay_end_date, is_local_hukou, proof_type,
            created_by, updated_by
        ) VALUES (
            #{name}, #{idCard}, #{gender}, #{birthday}, #{phone}, #{actualAddress}, #{addressProvince}, #{addressCity}, #{addressDistrict}, #{addressDetail}, #{residenceType}, #{status},
            #{stayStartDate}, #{stayEndDate}, #{isLocalHukou}, #{proofType},
            #{createdBy}, #{updatedBy}
        )
    </insert>

    <update id="updateById" parameterType="com.population.resident.domain.Resident">
        UPDATE resident
        SET
            name = #{name},
            id_card = #{idCard},
            gender = #{gender},
            birthday = #{birthday},
            phone = #{phone},
            actual_address = #{actualAddress},
            address_province = #{addressProvince},
            address_city = #{addressCity},
            address_district = #{addressDistrict},
            address_detail = #{addressDetail},
            residence_type = #{residenceType},
            status = #{status},
            stay_start_date = #{stayStartDate},
            stay_end_date = #{stayEndDate},
            is_local_hukou = #{isLocalHukou},
            proof_type = #{proofType},
            updated_by = #{updatedBy}
        WHERE id = #{id}
          AND is_deleted = 0
    </update>

    <update id="logicalDelete">
        UPDATE resident
        SET is_deleted = 1,
            updated_by = #{updatedBy}
        WHERE id = #{id}
          AND is_deleted = 0
    </update>

    <update id="updateJudgeResult">
        UPDATE resident
        SET residence_status = #{residenceStatus},
            residence_score = #{residenceScore},
            judge_version = #{judgeVersion},
            judge_reason = #{judgeReason},
            last_judge_time = NOW(),
            updated_by = #{updatedBy}
        WHERE id = #{id}
          AND is_deleted = 0
    </update>

    <update id="updateManualJudge">
        UPDATE resident
        SET residence_status = #{residenceStatus},
            judge_reason = #{judgeReason},
            judge_version = 'manual',
            last_judge_time = NOW(),
            updated_by = #{updatedBy}
        WHERE id = #{id}
          AND is_deleted = 0
    </update>
</mapper>
