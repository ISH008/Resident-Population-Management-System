<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.population.resident.mapper.SysRoleMapper">

    <select id="findRoleCodesByUserId" parameterType="long" resultType="string">
        SELECT r.role_code
        FROM sys_user_role ur
        JOIN sys_role r ON ur.role_id = r.id
        WHERE ur.user_id = #{userId}
          AND r.status = 1
    </select>

    <select id="findRoleIdsByUserId" parameterType="long" resultType="long">
        SELECT ur.role_id
        FROM sys_user_role ur
        JOIN sys_role r ON ur.role_id = r.id
        WHERE ur.user_id = #{userId}
          AND r.status = 1
    </select>

    <select id="findAllEnabled" resultType="com.population.resident.domain.SysRole">
        SELECT id, role_code, role_name
        FROM sys_role
        WHERE status = 1
        ORDER BY id ASC
    </select>

    <select id="countByRoleId" parameterType="long" resultType="int">
        SELECT COUNT(1)
        FROM sys_role
        WHERE id = #{roleId}
          AND status = 1
    </select>
</mapper>
